# Calculator panel fixes (CALC agent)

- item 4 (formatting): src/notation/calcString.ts rewritten around one dialect printer. / and ^ operands are wrapped only when not a single number, symbol or function call ("1/4*x", "x^2", "(x-3)/(-3)"); nested ParenthesisNodes that compileExpression leaves behind are peeled (fixes "(((x - 2))^(3)-10)/(3)"); sums keep the parens precedence needs (right of −, factors, negation operand). TI-84 gets the same policy plus "(3/4)X" for a quotient used as an implied factor. No fractional powers (cbrt → ³√( / root(u,3)).
- item 1 (number-line): new branch in ti84.ts and nspire.ts. Store-and-test at probeValues(boundary): TI-84 value STO→ X,T,θ,n, predicate typed with 2nd MATH TEST / LOGIC, answer 1/0; Nspire Scratchpad Calculate, ctrl var (→), ctrl = palette, words and/or, answer true/false. Predicate rendered per calculator via predicateFor → renderRelation(parseRelation(...)), chains split into "and", AND groups parenthesised inside "or". (−) key cautions. No inverse text.
- item 2 (compound inequality): relations that are not a single comparison now graph middle + both bounds (TI-84 Y1/Y2/Y3, Nspire f1/f2/f3, "true between the lines") when there is one middle expression (betweenOf), then store-and-test k and the probes around the crossings (linearCrossings; falls back to boundary or k). Single-comparison path untouched.
- item 3 (asymptotes): inverse-rational / inverse-mobius get an "Asymptotes swap" step on both calculators (x = v, y = h → inverse x = h, y = v, fmtEndpoint). TI-84: 2nd ZOOM (FORMAT) → Detect Asymptotes (OS 5.2+), placed before DrawInv since a FORMAT change erases the drawing. Nspire: breaks automatically; optional f5/f6 horizontal lines.
- also: TI-84 plus-minus-sqrt no longer prints f itself as the "positive branch" when inverseExpr is missing (uses √(pmInner) or skips); Nspire panels with no inverse (function without inverseExpr, plus-minus-sqrt) no longer reference an undefined f2 in the check step.
- item 5 (tests): src/content/calc/calc.test.ts added (12 tests): every CalcFamily x both calculators over 8 seeds of each template (ineq.distribute/compound/fraction, nl.read, evenOdd.poly, inv.linear/cbrt-shift/frac-linear/rational/mobius/quadratic-not) plus hand-written function and plus-minus-sqrt instances; checks non-empty steps, no {{ / undefined / NaN / Infinity / fractional powers, k mentioned, number-line store-and-test with per-calculator predicate and no inverse text, single-inequality left-right boundary, compound between-the-lines + both interval endpoints probed, not-one-to-one k + twin, rational/mobius asymptotes both ways, plus helper tests (predicateFor, betweenOf, linearCrossings). src/notation/calcString.test.ts gains the parenthesis-policy tests; its old "calc templates" block (upward import notation -> content) moved into calc.test.ts. Nspire cube-root caution reworded so no text contains ^(1/3). npm run typecheck clean; npm test 25 files / 231 tests pass (was 215).
