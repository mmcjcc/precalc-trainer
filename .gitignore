node_modules
dist
dist-ssr
*.local
.env
.env.*
!.env.example
.DS_Store
coverage
.vite

# Secrets: never commit keys (the repo is public). Keep them outside the repo; Azure holds the live copies.
apikey*
*.apikey
client_secret*.json
secrets/
